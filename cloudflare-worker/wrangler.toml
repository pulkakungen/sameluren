name = "sameluren-push"
main = "src/worker.js"
compatibility_date = "2026-01-01"

# Skapas med: npx wrangler kv namespace create PUSH_KV
# Klistra in det "id" som kommandot skriver ut här:
kv_namespaces = [
  { binding = "PUSH_KV", id = "20398c84443e45958962d3bde1505a40" }
]

# Kör var 15:e minut, dygnet runt (worker-koden filtrerar själv på svensk lokaltid)
[triggers]
# Var 15:e minut för notiserna, plus 21.58 och 22.58 UTC så att en av dem
# alltid är 23.58 svensk tid, då dagens rad skrivs till kalkylarket.
crons = ["*/15 * * * *", "58 21,22 * * *"]

# Hemligheter (sätts med `npx wrangler secret put <NAMN>`, skrivs ALDRIG i denna fil):
#   VAPID_PRIVATE_KEY
#   VAPID_PUBLIC_KEY
#   VAPID_SUBJECT   (t.ex. "mailto:din@epost.se")
